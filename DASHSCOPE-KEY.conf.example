# Copy to DASHSCOPE-KEY.conf (gitignored) and paste your Qwen key.
# Never on argv. The DASHSCOPE_API_KEY environment variable wins if both exist.
#
#   cp DASHSCOPE-KEY.conf.example DASHSCOPE-KEY.conf
#
# One line is enough — either the bare key or:

DASHSCOPE_API_KEY=
