# Copy to DEEPSEEK-KEY.conf (gitignored) and paste your key.
# Never on argv. The DEEPSEEK_API_KEY environment variable wins if both exist.
#
#   cp DEEPSEEK-KEY.conf.example DEEPSEEK-KEY.conf
#
# One line is enough — either the bare key or:

DEEPSEEK_API_KEY=
