# Soft bar: GPT-2 12×4 last-4, margin=0.015

Same LRs as [../2026-08-17-blind-12x4-k4/](../2026-08-17-blind-12x4-k4/). Hit if `marked_lr + 0.015 >= unmarked_lr`. **11/12**. Station flips (gap was 0.010). Office still misses (gap 0.066).

`used_keys=false`. This is a lower bar, not a stronger model. Strict remains 10/12. Margin 0.08 would be 12/12 and would count office — that miss is not close.
