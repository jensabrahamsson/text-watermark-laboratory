# 100 one-liners → Grok-register 12×4, occupancy-free postokhits

Same flags as lock B, `--methods postokhits`. Not a new method name.
`used_keys=false`.

Prompt ranking **10/12** is empty at isolated grain: all ten ranking
wins have 0 marked files with `lr>0`. Nested Youden **0/48 vs 48/48**.
t=0 **0/48 vs 48/48**. Opening coverage on these tables is **5/48**
(0 exact 4-token copies); those five covered files are not isolated TPs
(negative observed-token LR). Do not sell 10/12. Does not replace
**25/48**.
