# Cascade on the 24-short OOD gate with prompt-conditioned pivot

`--prompt-context` on the pivot only. Cascade count tables stay
isolated-file (no prompt): **16/48**, precision **1.000**. Pivot
fallback 9/32. Combined 25/48. Prompt-conditioned LDA is chance.
Not an isolated `indicate score` protocol.

Write-up: [../../research/key-free-cascade.md](../../research/key-free-cascade.md).
