# Opening-overlap bound: 100 one-liners → Grok-register 12×4

Observed-token isolated recall is bounded by train opening-atom overlap.
Not a new `probe --methods` name. `used_keys=false`.

`postokhits` covers **5/48** marked files (exact 4-token copy **0/48**;
all five are last-1 only). Occupancy-free t=0 is **0/48 vs 48/48**: the
five covered files have negative observed-token LR. Does not replace
**25/48**.
