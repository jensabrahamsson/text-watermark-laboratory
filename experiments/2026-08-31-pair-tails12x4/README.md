# 2026-08-31 — tail-transplant GPT-2 prompts × 4 draws × 128 tokens

Seeds: [`../2026-08-31-prompts-tails12/`](../2026-08-31-prompts-tails12/).
Three new bodies (ice rink, bakery night, county archive) with last
paragraphs taken from the 12×4 stems whose openings were zeros.
Not Claude. Not copies of the original twelve as wholes.

Seed `20260902`. `n_samples=4`. Official first-draw lamp **12/12**.
Min marked mean ≈ 0.612. Max unmarked mean ≈ 0.516.

Generated token 0 on 48 marked files: `The` 41, `"` 4, `After` 2, `Now` 1.
**No `Closing`.** One depot-tail draw is exactly `After two and a`
(night-bus). One letter-tail draw is `Now a little after` (garden).
Transplanting the library last paragraph did **not** copy `Closing is the`.
