# Tail-transplant GPT-2 pair seeds (31 Aug 2026)

Three new scene bodies (ice rink, bakery night, county archive) each
end with one last paragraph taken from a 12×4 seed whose marked
openings were zeros on the isolated-file gate:

- closing ← `03-library` last paragraph
- depot ← `02-night-bus` last paragraph
- letter ← the whole short `08-letter` seed
- garden ← the whole short `11-garden` seed

New bodies. Not Claude. Not copies of the original twelve as wholes.
The question is whether GPT-2's first generated tokens follow the
**prompt tail**, so that train twins with those tails cover After /
Closing / Now / While on the original 12×4 files. That is still not a
universal detector: covering a prompt-idiosyncratic opening by training
on the same tail is close to in-family.
