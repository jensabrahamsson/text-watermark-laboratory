# GPT-2 12×4 twins, public keys, ngram_len=13

Frozen in [PROTOCOL-next-longctx.md](../../research/PROTOCOL-next-longctx.md)
(`b70986d`). Original 12 Grok prompt strings, new twins, seed
**20260903**, $T=128$, 4 draws. Official scoring uses matching
`ngram_len=13`. First-draw marked means are all above 0.55. This is
not `public-deepmind-30` at `ngram_len=5`.
Hub revision was not recorded at generation. Published scores read these
committed strings. Future `pair` runs can pass `--hub-revision`.
