# Opening-overlap bound: Grok-register 12×4 → original 12×4

Observed-token isolated recall equals train opening-atom overlap.
Not a new `probe --methods` name. `used_keys=false`.

`postokhits` covers **5/48** marked files (exact 4-token copy **0/48**;
all five are last-1 only). Decided 5 TP / 1 FP. Occupancy-free t=0 is
**5/48 vs 47/48**. The leftover zeros are still `The ferry` / `Closing`
/ `The dog` / `The printer` / `Now` / `While` openings these 12 new
scenes do not produce. Does not replace **25/48**.
